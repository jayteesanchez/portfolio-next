{
  "name": "resume-access",
  "version": "1.0.0",
  "description": "Lambda that validates an email, notifies via SES, and returns a pre-signed S3 URL for the resume PDF.",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "zip": "npm run build && cd dist && zip -r ../resume-access.zip index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-ses": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
